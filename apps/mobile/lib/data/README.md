# data

Local persistence (Drift database, DAOs, the operation log and materializer)
and remote data sources (the generated API client) — the implementations
behind the interfaces declared in `domain`.
